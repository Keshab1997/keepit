// NOTE: lucide_icons 0.257.0 originally defined a `LucideIconData extends
// IconData` helper here. Flutter made `IconData` a `final class`, so extending
// it is no longer allowed. Icons are now constructed as plain `IconData`
// values directly in lucide_icons.dart (same fontFamily/fontPackage), which
// renders identically.
