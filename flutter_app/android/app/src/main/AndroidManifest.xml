<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.keshabstudios.keepit">
    
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Serendipity reminders: re-schedule after reboot / app update -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Android 11+ package visibility: open links in browsers / other apps and
         compose support emails (url_launcher). -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>

    <application
        android:label="KeepIt"
        android:name="com.keshabstudios.keepit.CrashApp"
        android:icon="@mipmap/ic_launcher">
        
        <!-- AdMob app ID. Injected by Gradle from the ADMOB_APP_ID build
              environment variable (manifestPlaceholders["admobAppId"]);
              the real value lives in a GitHub Actions secret and is never
              committed. See docs/SECRETS_SETUP.md. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}" />

        <activity
            android:name="io.flutter.embedding.android.FlutterActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
              
            <!-- Default Launcher Intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Share Target Intent Filter (Instagram, YouTube, Chrome, etc.) -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <!-- flutter_local_notifications: scheduled Serendipity reminders -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- Handles "Mark Watched" / "In 1 Week" notification buttons -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />

        <!-- Tells Flutter this app uses the Android v2 embedding.
             Without this meta-data, builds fail with "deleted Android v1 embedding". -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Crash safety net: shows uncaught Java/native crashes (which Dart
             try/catch can never catch) in a readable screen, in a separate
             process so it survives the crashing one. -->
        <activity
            android:name="com.keshabstudios.keepit.CrashActivity"
            android:process=":crash"
            android:theme="@android:style/Theme.Light"
            android:exported="false" />

        <!--
             KeepIt is local-first: Firebase and ads are optional, and CI
             builds ship without google-services.json. These auto-init
             providers run at process start — before any guarded Dart code —
             and can kill the app before the first frame. Removing them keeps
             startup entirely in Dart's hands (FirebaseBootstrap, AdService).
        -->
        <provider
            android:name="com.google.firebase.provider.FirebaseInitProvider"
            tools:node="remove" />
        <provider
            android:name="com.google.android.gms.ads.MobileAdsInitProvider"
            tools:node="remove" />
    </application>
</manifest>
